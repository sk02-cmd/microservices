apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.auth.name }}
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: auth
  ports:
    - port: 80
      targetPort: {{ .Values.auth.port }}